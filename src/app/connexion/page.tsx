"use client";
import { FormEvent, useState } from "react";
import Link from "next/link";
import { useRouter } from "next/navigation";
import { ArrowLeft } from "lucide-react";
export default function LoginPage() { const router = useRouter(); const [error, setError] = useState(""); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setError(""); const form = new FormData(event.currentTarget); const response = await fetch("/api/auth/login", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ email: form.get("email"), password: form.get("password") }) }); if (!response.ok) { const result = await response.json(); setError(result.error); } else { const next = new URLSearchParams(window.location.search).get("next"); router.push(next || "/admin"); } } return <main className="mx-auto max-w-md px-5 py-16"><Link href="/" className="inline-flex items-center gap-2 text-sm text-[#77746d]"><ArrowLeft size={16}/> Accueil</Link><h1 className="mt-16 text-4xl font-bold tracking-[-.06em]">Connexion</h1><p className="mt-3 text-[#77746d]">Accédez à votre espace ASKAAN.</p><form onSubmit={submit} className="mt-8 space-y-5"><label className="block text-sm font-bold">Email<input required name="email" type="email" className="mt-2 w-full rounded-xl border border-[#d9d6cd] bg-white/60 px-4 py-3.5"/></label><label className="block text-sm font-bold">Mot de passe<input required name="password" type="password" className="mt-2 w-full rounded-xl border border-[#d9d6cd] bg-white/60 px-4 py-3.5"/></label>{error && <p className="text-sm text-red-700">{error}</p>}<button className="w-full rounded-full bg-[#161616] px-5 py-4 font-bold text-white">Se connecter</button></form></main> }
