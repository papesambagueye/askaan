import { notFound } from "next/navigation";
import Image from "next/image";
import Link from "next/link";
import { ArrowLeft, Copy, Heart, Share2 } from "lucide-react";
import { formatCFA, getPublishedCampaigns, percent } from "@/lib/campaigns";
import { Header } from "@/components/Header";
export async function generateStaticParams() { return []; }
export default async function CampaignPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const campaign = (await getPublishedCampaigns()).find(item => item.id === id); if (!campaign) notFound(); const progress = percent(campaign.raised_cfa, campaign.goal_cfa); return <><Header/><main className="mx-auto max-w-6xl px-5 pb-20 pt-8 lg:px-8"><Link href="/#campagnes" className="mb-8 inline-flex items-center gap-2 text-sm font-semibold text-[#77746d]"><ArrowLeft size={16}/> Retour aux collectes</Link><div className="grid gap-10 lg:grid-cols-[1.1fr_.9fr]"><div><div className="relative aspect-[1.2] overflow-hidden rounded-3xl bg-[#e9e4d8]">{campaign.image_url && <Image src={campaign.image_url} alt={campaign.title} fill priority className="object-cover" sizes="(max-width: 1024px) 100vw, 60vw"/>}</div><div className="mt-6 flex items-center gap-3"><button className="flex items-center gap-2 rounded-full border border-[#d9d6cd] px-4 py-2 text-sm font-semibold"><Share2 size={15}/> Partager</button><button className="rounded-full border border-[#d9d6cd] p-2" aria-label="Copier le lien"><Copy size={15}/></button></div></div><div className="flex flex-col justify-center"><span className="mb-4 w-fit rounded-full bg-[#f7c844] px-3 py-1 text-xs font-bold">{campaign.category}</span><h1 className="text-4xl font-bold leading-[.98] tracking-[-.06em] md:text-6xl">{campaign.title}</h1><p className="mt-5 leading-relaxed text-[#77746d]">{campaign.description}</p><div className="mt-8 progress"><span style={{ width: `${progress}%` }}/></div><div className="mt-4 flex justify-between"><div><p className="text-2xl font-bold">{formatCFA(campaign.raised_cfa)}</p><p className="text-sm text-[#77746d]">collectés sur {formatCFA(campaign.goal_cfa)}</p></div><div className="text-right"><p className="text-2xl font-bold">{progress}%</p><p className="text-sm text-[#77746d]">{campaign.supporters} soutiens</p></div></div><a href={campaign.payment_url} target="_blank" rel="noreferrer" className="mt-8 flex items-center justify-center gap-2 rounded-full bg-[#161616] px-6 py-4 font-bold text-white transition hover:bg-[#333]"><Heart size={18} fill="currentColor"/> Soutenir via Wave / Orange Money</a><p className="mt-3 text-center text-xs text-[#77746d]">Vous serez redirigé vers le lien de paiement du porteur.</p></div></div></main></> }
