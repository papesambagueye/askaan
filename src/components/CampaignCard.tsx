import Image from "next/image";
import Link from "next/link";
import { ArrowUpRight } from "lucide-react";
import { Campaign, formatCFA, percent } from "@/lib/campaigns";
export function CampaignCard({ campaign }: { campaign: Campaign }) { const progress = percent(campaign.raised_cfa, campaign.goal_cfa); return <Link href={`/campagne/${campaign.id}`} className="group card block overflow-hidden transition hover:-translate-y-1 hover:shadow-xl"><div className="relative aspect-[1.35] overflow-hidden bg-[#e9e4d8]">{campaign.image_url && <Image src={campaign.image_url} alt={campaign.title} fill className="object-cover transition duration-500 group-hover:scale-105" sizes="(max-width: 768px) 100vw, 33vw"/>}<span className="absolute left-3 top-3 rounded-full bg-[#f7c844] px-3 py-1 text-xs font-bold">{campaign.category}</span></div><div className="p-5"><h3 className="min-h-[3.5rem] text-lg font-semibold leading-tight tracking-[-.03em]">{campaign.title}</h3><div className="mt-5 progress"><span style={{ width: `${progress}%` }}/></div><div className="mt-3 flex items-end justify-between"><div><p className="text-lg font-bold">{formatCFA(campaign.raised_cfa)}</p><p className="text-xs text-[#77746d]">sur {formatCFA(campaign.goal_cfa)}</p></div><span className="flex items-center gap-1 text-sm font-bold">{progress}% <ArrowUpRight size={15}/></span></div><p className="mt-4 text-xs text-[#77746d]">{campaign.supporters} contributeurs</p></div></Link> }
